package DariKawan.Daffarel;

public class coba {
    @SuppressWarnings("resource")
    public static void main(String[] args) {
        // Scanner x = new Scanner(System.in);
        // String s = x.nextLine();
        // int length = s.trim().length()+1;
        // length += 1;
        // String[] sar = new String[s.split(" ").length];
        // sar = s.split(" ");
        // for(int i = 0; i < sar.length; i++){
        //     System.out.println(sar[i]);
        // }
        // length = length - (s.split(" ").length - 1);
        // System.out.println(length);
        // System.out.println(s.trim().length()+1);
        // System.out.println(x.nextLine().trim().length()+1);
        System.out.println(new java.util.Scanner(System.in).nextLine().trim().length()+1);
        // x.close();
    }
}
